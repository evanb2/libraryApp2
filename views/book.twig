<html>
<head>
  <title>Library Database</title>
  <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
</head>
<body>
  <div class='container'>
    <h1>{{ book.getTitle }}</h1>

    {% if patrons is not empty %}
      <p>Here are all Patrons in the Database:</p>
      <ul>
        {% for patron in patrons %}
          <li>{{ patron.getName }} : {{ patron.getPhone }}</li>
        {% endfor %}
      </ul>
    {% endif %}

    <!-- <h4>Edit this Book:</h4>

    <form action='/add_patrons' method='post'>
      <input id='book_id' name='book_id' type='hidden' value='{{ book.getId }}'>
      <label for='book_id'>Select a Patron</label>
      <select id='patron_id' name='patron_id' type='text'>
        {% for patron in all_patrons %}
          <option value='{{ patron.getId }}'>{{ patron.getName }}</option>
        {% endfor %}
      </select>
      <button type='submit'>Add Patron</button>
    </form> -->
    <p><a href='/books/{{ book.getId }}/edit'>Edit this Book</a></p>
    <p><a href='/'>Home</a></p>
  </div>
</body>
</html>
